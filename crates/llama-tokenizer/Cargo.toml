[package]
name = "llama-tokenizer"
description = "Tokenizer crate for llama.rs — deterministic text-to-token conversion"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
